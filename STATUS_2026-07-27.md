# 资源库现状报告（2026-07-27 晚）

> 实际操作了一次内容更新 + 全量审计，结论如下。所有硬不变量通过。

## 一、数据规模与不变量

| 指标 | 值 |
|---|---|
| 论文总数 | **1893** 篇（本轮 +1，增量抓取新候选） |
| `data/papers.json` ↔ `docs/papers.json` md5 | ✅ 一致 |
| CCF 与 `ccf.json` 一致 | ✅ |
| venue 孤儿链接 | 0 |
| 缺失字段 / 重复 | 无 / 无 |

当前 `data/papers.json` md5：`f401233e1fd6273fadd105f7bb842de9`

## 二、内容覆盖率

| 维度 | 覆盖 | 说明 |
|---|---|---|
| 摘要（abstract） | **73%**（1391/1893） | arXiv+Crossref 真实天花板；剩余为 ACL/CVF 等仅 PDF、无结构化摘要的会议论文 |
| 代码链接（code） | **5.3%**（100/1893） | 字段在 `paper["code"]`；`code_links.json` 有 148 条，约 48 条因标题归一化差异未命中 |

> 注：之前误报"代码链接 0%"是统计查错了字段（查了 `links` 而非 `code`）；"摘要富集白跑"也是误判——Crossref 的 166 条摘要已确认进入产物。

## 三、库画像

- **模型类型**：LLM 1126 · VLM 719 · MLLM 47
- **年份**：2026: 723 · 2025: 763 · 2024: 364 · 2023: 24（2027 已有 5 篇预印本）
- **CCF 评级**：未收录 1168 · A 433 · B 223 · C 68

## 四、本轮实际做的更新动作

1. **增量抓取**：`fetch_new_arxiv.py` → 1 篇新候选进入 `incremental.json`（822 条），`generate` 后入库 1893 篇。
2. **摘要富集复核**：`fetch_abstracts`（arXiv 缓存已全，0 新抓）+ `enrich_abstracts`（Crossref 本次 0 新增，历史 166 条已生效）。确认摘要覆盖率停在 73% 是数据天花板，非 bug。
3. **代码链接验证**：`fetch_code --limit 50` 验证管线能写 `code_links.json`；但当前代理下 `api.github.com` 疑似不通，逐篇超时重试极慢，已中断（不影响产物，原子写保证缓存一致）。

## 五、遗留 / 下一步建议

- **代码链接偏低（5.3%）**：受 GitHub 匿名限流（60/hr）+ 当前代理不通影响。若要提升：设置 `GITHUB_TOKEN` 且代理放行后跑 `fetch_code.py`（全量 ~1893 篇需数小时，建议后台）。
- **48 条 code_links 未命中**：`code_links.json` 的 key 与 `generate` 用的 `norm_title(title)` 因历史标题归一化差异有少量不match，可统一 key 策略补回。
- **仓库无 git**：所有改动（含之前多轮优化）仍在工作区，未纳入版本管理。
- **可选自动化**：定时跑 `update_pipeline.py --check`（离线只读审计门禁），把"校验"闭环。

## 六、环境说明

- arXiv API 当前返回 200（之前的代理 502 风险暂时不存在）。
- GitHub API 在当前代理下疑似不通，影响 `fetch_code` 全量运行。
