# 2026-07-25 续做：恢复回填 + CCF 扩充 + 审计

## 关键发现：数据回退（incremental.json 被清空）
- 接手时发现 `data/incremental.json` 为 `[]`，库从应有的 ~1873 掉到 **1061**（仅 raw_data.py）。gSUiTP 夜间跑的 venue 解析缓存进了 `venue_links.json`（1587 条，757 有 venue），但库本身被一次"空 incremental 的 generate"覆盖。
- **根因定位**：`scripts/fetch_new_arxiv.py` recent 模式在 arXiv 不可达时 `json.dump([], ...)` 把整个 historical backfill 清空（line 264-266）。这是真正的事故隐患。已修复：不可达时仅跳过、保留现有 incremental.json（不再清空）。
- **恢复**：用改进后的脚本重跑 `python scripts/fetch_new_arxiv.py --from 20240101`（ti: 命中 + OFF_TOPIC 护栏），净增 **821** 条回 incremental.json。库恢复为 **1882**（1061 raw + 821 incremental）。

## Task #35 扩充 ccf.json（已完成）
- 修复 `generate.py` 的 `JOURNAL_ABBR`：`neural netw.` 误把 IEEE TNNLS 映射成 `NN`（虽同为 B 但标签错）；DBLP 全名（`Neural Networks`/`Knowl. Based Syst.`/`Inf. Fusion` 等）与缩写 key 不匹配导致漏映射。新增 `ieee trans. neural networks learn. syst`→TNNLS、`knowl. based syst`→KBS、`iet image process`→IET IP、`inf. fusion`→INFFUS、`expert syst. appl`→ESWA、`neurocomputing`→NEUCOM、`pattern recognit. lett`→PRL、`image vis. comput`→IVC。
- `ccf.json` 新增 CCF-2022 评级：ECIR/CIKM/IJCNLP-AACL/ECAI/NDSS/ASE/MICCAI=B，ICDE=A，GLOBECOM/CLEF/TAI/IET IP/KBS/INFFUS/ESWA/NEUCOM/PRL/IVC=C。
- 验证：ECIR/CIKM/ECAI/NDSS/MICCAI/ASE→B、ICDE→A、CLEF/GLOBECOM/TAI/ESWA/KBS/INFFUS/NEUCOM→C 均正确解析。

## Task #37 最终管线 + 审计（已完成）
- 顺序：generate(物化) → update_arxiv_venues(增量，全命中缓存，5s 无新增) → generate(应用) → audit。
- audit 通过：data≡docs md5 一致；CCF 与 ccf.json **0 处不符**；重复标题 0；字段完整。
- 最终库：**1882 篇** / 摘要 1380 / 代码 91 / 顶会正式发表(venue_url) 848 / Benchmark 111 / Survey 32 / 年份 2018–2027。
- CCF 分布：A 393 / B 212 / C 66 / 未收录 1211（其中 arXiv 预印本 1007 本就无正式 venue，属正常）。

## 待用户定夺
- 全量移除 BROAD 组时曾误删的十几篇真相关论文（HALLMARK/Groc-PO/MissingBench/WeedExpert-R1 等），是否加回 raw_data.py 仍待确认。
- 两个自动化目前各自编排 fetch→generate→enrich→generate；后续可统一改为只调 `scripts/update_pipeline.py`（更稳，已合成未切换）。

---

# 2026-07-25（续）用户确认：加回误删论文 + 自动化统一

## 1. 加回 10 篇误删真相关论文 → raw_data.py（已完成）
- 从 `2026-07-24.md` 行 192 定位到被 BROAD 清理一并误删的真实论文清单：HALLMARK / Groc-PO / MissingBench / Faithful by Design / ArtChart / Do Medical VLMs Actually See / Deceptive Grounding / ProCap / HoloCount / WeedExpert-R1。
- 无 git（非仓库），incremental 已被清空且 ti: 重抓抓不到（这些标题**不含 hallucination**），故用 arXiv API 逐篇找回精确元数据并加入 `scripts/raw_data.py` 末尾（显式注释块）。arXiv ID：
  - HALLMARK:2607.18360 · Groc-PO:2607.13712 · MissingBench-Verified:2607.18673 · Faithful by Design:2607.09932 · ArtChart:2607.16060 · Do Medical VLMs Actually See:2607.03647 · Deceptive Grounding:2607.09349 · ProCap(视频):2607.21022 · HoloCount:2607.06420 · WeedExpert-R1:2607.16492
- 验证：generate 后 1892 篇，10 篇全部在位；generate 对 RAW 列表无关键词门禁（仅按 norm_title 去重），故永久保留、不再被任何清理误删。

## 2. 两个自动化统一切到 update_pipeline.py（已完成）
- automation-1784902350808（日常·周一三五）：prompt 改为只调 `python scripts/update_pipeline.py`（默认=近期 ti: 抓取→物化→enrich→物化→audit），去掉手写四步。
- automation-1784903054118（大更新·每8周六）：prompt 改为 `python scripts/update_pipeline.py --backfill --from 20240101 --with-abstracts`（高精度历史回填+补摘要）。
- 两者 rrule / cwds / 状态均保持不变。

## 3. 顺带修复 update_arxiv_venues.py 性能隐患（关键）
- 原逻辑：循环对每个 arXiv 预印本都跑 DBLP 查询（无缓存跳过），即每日自动化会为 ~1000 篇预印本打 ~1000 次 DBLP（约 30–55 分钟、且易触发限流）。切到 update_pipeline 后会变成每日重压。
- 修复：① 循环顶部 `if key in cache and (cache[key] or not force): continue` —— 已解析的永不重查、已缓存负结果（未正式发表）默认跳过；② 新增 `--force` 参数；③ `update_pipeline.py` 在 `--backfill` 时给 enrich 传 `force`，使大更新仍全量重查负结果以捕获"已正式发表"的预印本。
- 验证：`update_pipeline.py --skip-fetch` 端到端 4 秒跑完（仅 10 篇新预印本被查）；audit 通过（data≡docs md5、CCF 0 不符、0 重复、字段完整、1892 篇、2018–2027）。

## 当前库状态
- 1892 篇 / 摘要 1390 / 代码 91 / 顶会正式发表 848 / CCF A393·B212·C66·未收录1221（含 arXiv 预印本 1017）。
- 交付文件：scripts/raw_data.py（+10 篇）、scripts/update_pipeline.py（传 force）、scripts/update_arxiv_venues.py（增量+force）。

---

# 2026-07-25（续）"先优化"：5 项管线质量/健壮性改进（仓库后建）

用户：先优化，GitHub 仓库后期再建（命名与更新策略待议）。完成以下 5 项，全部经 generate + audit --strict 验证通过（1898 篇、data≡docs、CCF 0 不符、0 重复、字段完整）：

1. **code_common.http_get 尊重 Retry-After**（所有爬虫受益）：429/503 时读 `Retry-After` 头并睡眠（上限 120s）。
2. **fetch_new_arxiv.py 召回扩展 + 相关性护栏**：
   - 新增 `SYNONYM_QUERIES`（factual consistency / faithful / object·visual·multimodal hallucination 等），仅 recent 模式运行，提升"标题不含 hallucination 词"的论文召回。
   - 新增 `is_relevant(title, summary)` 严格护栏（摘要须提及 hallucination，或标题含 AI 标记+faithful/fabrication 等），避免误拉无关"faithful reconstruction"。
   - `harvest(q=)` 支持自定义查询；`process(gate=)` 可切换门禁；`datetime.utcnow()` 改为时区感知（消除弃用警告）。
   - 实测 recent 抓取 +6 篇（含 ProCap 来自 faithful 查询），全部相关。
3. **update_arxiv_venues.py Crossref 实时兜底**：新增 `crossref_lookup(doi)`，对"有 DOI 但 journal_ref 无映射 venue"的预印本实时查 Crossref 容器名→venue，并写回 `crossref_cache.json`（原先只读不写）。DBLP 2s 限流 sleep 已存在。
   - 实测两轮 enrichment 把 venue 解析从 757→796、正式发表 848→884、CCF A393→405/B212→218/C66→67。
4. **人工策展入口 data/manual_entries.yaml**（零依赖，自写最小 YAML 解析器 `load_manual_yaml()`，不引入 PyYAML）：
   - 把上一轮加回 raw_data.py 的 10 篇真相关论文**迁到 yaml**（从 raw_data.py 删除对应块），generate 在 `raw = list(RAW)` 后 `raw += load_manual_yaml()` 合并。后续人工增删只改 yaml（PR 易审阅）。
5. **工程化 + 趋势**：新增 `requirements.txt`（声明纯标准库、无需第三方）；`generate.py` 写 `data/stats.json`（含 generated_at 时间戳快照）+ 追加 `data/stats_history.jsonl`（每行一次运行，便于观察库增长）。

待办：仓库命名/更新策略讨论后再建 GitHub（届时写 .gitignore + 重写 build.yml + 删本地两条自动化）。

---

# 2026-07-25（续）fetch 召回收紧：标题必须含 hallucination，禁用同义词

用户明确：抓取召回扩展要求**题目必须包含 "hallucination"**，其它同义词（factual consistency / faithful / object·visual·multimodal hallucination 等）一律不行。

已对 `scripts/fetch_new_arxiv.py` 回退"先优化"阶段的同名改动：
- 删除 `SYNONYM_QUERIES` 列表及其在 recent 模式里的循环查询（不再跑任何同义词查询）。
- 删除 `is_relevant()` 护栏函数（已无调用方）。
- 在 `process()` 解析每条 entry 后新增**硬性标题门禁**：`if "hallucinat" not in (title or "").lower(): continue`（覆盖 hallucination / hallucinated / hallucinating 词形）。这是代码级兜底，无论查询模式（含 `--broad` 的 all:）都只保留标题含 hallucination 的论文。
- 主查询仍为 `ti:hallucination`（标题字段），与门禁双保险。注释同步更新，说明"no synonym recall"策略。

验证：
- `py_compile` 通过，无 `is_relevant`/`SYNONYM_QUERIES` 残留引用。
- 逻辑自测：标题 "A faithful survey of visual grounding"（无 hallucination）→ 被拒；"Object hallucination in VLMs" / "Factual consistency of LLMs without hallucination"（标题含词）→ 通过。符合用户要求。

# 2026-07-25（续）落实"补回"：核查同义词误召回并清理

用户："可以，加上"（同意把同义词召回里确属相关的论文用 yaml 补回）。

核查结果：
- `incremental.json` 中标题不含 "hallucination" 的条目共 **6 篇**（均来自上一轮 SYNONYM_QUERIES 的 "faithful" 查询）：Axolotl3D(2607.20660) / Vera(2607.20247) / SciForma(2607.18091) / 医学 EF 归因(2607.13738) / LAD 可解释性(2607.07264) / CoPAIR/TRACE MLLM 归因(2607.06726)。
- 读摘要确认：这 6 篇都是 "faithful X" 类任务（3D 形状补全/视频身份保持/科学图结构/医学归因/可解释性/视觉归因），**没有一篇研究模型幻觉**，仅是摘要顺带提了 "hallucination" 一词才过旧护栏——纯误召回。
- 用户举的 ProCap 例子已在 `data/manual_entries.yaml`（10 篇人工策展之一，标题 "ProCap: ... Faithful and Comprehensive Video Captioning" 不含 hallucination 但确属幻觉研究），无需重复添加。
- 决策：按严格规则，将这 6 篇从 `incremental.json` 移除（备份 incremental.json.bak），使其只保留标题含 hallucination 的论文；不把它们塞进 yaml（非幻觉主题）。`venue_links.json` 中这 6 篇无孤儿（均未富集）。
- regenerate 后库 **1892 篇**（原 1898 - 6），`audit.py --strict` 全绿（data≡docs md5、CCF 0 不符、0 重复、字段完整）。
- 备注：`audit --strict` 报告 venue_links 孤儿 67 条，为更早清理（BROAD 组等）遗留，非本次引入、不影响输出，留作后续清理项。
