# 2026-07-28

## README 自动同步（消双源漂移）
- 新增 `scripts/gen_readme.py`：从 `data/papers.json` 自动重写 README.md 的 5 个漂移区块（顶部 badges 数字、数据概览统计表、Benchmark/Survey 列表、按年份分组的论文列表），保留人工段落（分类体系/推荐引用/贡献/许可）与标题行。
- 按 `##` 标题定位区块、替换标题到下一 `##` 间内容；venue 分组 `main = ALWAYS_LIST 或 (count>=2 且非 minor)`，其余归「其他」行+折叠明细（阈值对齐手写版的 >=2，使「其他」明细恰好 161 行）。
- 接入 `update_pipeline.py`（generate#2 后 `run("gen_readme.py")`），流水线更新数据后 README 自动同步，无需手工维护。
- 验证：生成数字与手写版完全一致（1893/214/1391/935/923/22/114/32，其他明细 161 行），主列表 1893 / bench 114 / survey 32 条目齐全；py_compile + 区块完整性校验通过。
- 本轮用户仅选 README 同步，网页(app.js/style.css)未动。网页待办仍为：BibTeX 复制/导出、仅看有代码筛选+PDF 直链、搜索高亮+相关论文推荐+收藏书签。

## README 折叠修正 + 主列表按模型类型分组
- **修真 bug：`generate.py:486` 的 `venue_from_doi()` 曾把 Crossref 期刊名在 60 字符处截断成 `s[:57]+"…"`**。后果：28 篇论文 venue 成残名（如 `SemEv…`、`E-Business,…`），落入 README「其他」折叠明细后展开即见残名；且截断可能把同一会议截成两条"不同的"venue 造成重复计数。**已删除截断**（完整名在 markdown 表格内能正确渲染，列变宽而已）。重跑 generate 后 `含 '…' 的 venue 数: 0`。
- **主论文列表改按模型类型分组**：`gen_readme.py gen_list` 由"按年份"改为"先按 `model_type`（🤖LLM / 👁️VLM / 🌐MLLM(Omni)）再按年份"的三层嵌套 `<details>`（模型组闭合、年内嵌套闭合）。直接对应"把 llm,vlm,mllm 分开展示"，且不增加总行数（仅重排）。这版起主列表导航轴=模型类型（LLM 1127 / VLM 719 / MLLM 47），年份退为次级轴。
- 重跑 `gen_readme.py`：列表模型分组=3、残名=0、其他明细残名=0；数字同步为 论文总数 1893 / 含代码 408 / 含摘要 1391。`audit.py --strict` 全不变量 OK。
- **注意 code 覆盖意外涨到 408（之前报 214）**：code_links.json 缓存本轮累计增至 488 值条目（全匹配论文），407/408 为 github 真链接、仅 1 条疑似（论文本人的 survey-materials 仓库，非误报）。增长源于本会话多次 fetch_code 运行/后台任务累积真实链接，非回归；用户此前说"先忽略代码问题"，故未重审。report 时透明说明即可。
