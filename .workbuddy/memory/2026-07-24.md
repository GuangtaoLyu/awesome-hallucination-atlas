# 2026-07-24 工作日志

## 十二轮：README 添加推荐引用区块（原仓库三篇 Lyu 论文 + BibTeX）
- 用户要求把"之前 README 里列出来的三篇"作为推荐引用并附 bib。本地无备份，经确认后从原始 GitHub 仓库（GuangtaoLyu/awesome-hallucination-mllm）抓取：原仓库在 Paper List 开头列了作者 Lyu Guangtao 自己的三篇论文 + BibTeX。
- 三篇（均已在库中）：
  1. lyu2026hallu_sae — Towards Interpretable Hallucination Analysis and Mitigation in LVLMs via Contrastive Neuron Steering (arXiv:2602.00621, 2026)
  2. lyu2025hallu_vdc — Revealing Perception and Generation Dynamics in LVLMs: ... Validated Dominance Correction (arXiv:2512.18813, 2025)
  3. lyu2026hallu_pade — Revealing and Enhancing Core Visual Regions: ... (ACL Findings 2026；库内经 venue_links 解析为 ACL 2026, findings-acl.748)
- 实现：`generate.py` 在「🤝 贡献」区块前新增「📖 推荐引用」区块（硬编码三段 bibtex，原样保留原仓库写法），重新生成 README。数据统计不变（1055 篇）。

## 十三轮：补采 CVPR2026（用户指出 CVPR 仅 24 篇太少）+ 网站按会议筛选
- 根因：CVF `?day=all` 页 12MB/4068 篇，旧 30s 超时抓不完；且 2026 页面改版（作者从 `<dd class="auth">` 变为 query_author 表单），旧正则 0 命中 → CVPR2026 全漏。
- 修复 `collect_candidates.py`：超时 30→300s；解析改为按 ptitle 切块、兼容新旧两种作者结构。
- 新增 `scripts/collect_2026.py`：CVF CVPR2026 专项 + DBLP 全 venue 2026 重扫。结果：官网标题含 hallucinat 39 篇（与用户手搜一致）；35 篇已在库但标为 arXiv → 写入 venue_links.json 官方映射（+37）；4 篇全新；DBLP 2026 重扫 0 新增（TPAMI/IJCV/BMVC 查询失败，限流）。
- 修 bug：硬门槛 `"hallucination" not in title` 误杀动词形式（FINER: MLLMs Hallucinate...），collect/merge 均改为词干 `"hallucinat"`；FINER 已补入。HalluGen（image restoration 幻觉合成）按既定范围排除。
- 结果：1055→1060；CVPR 24→62（2024:6/2025:18/2026:38）；arXiv 286→259；未标注 27→21；CCF-A 333→371；with_venue 586→624。
- 网站：`docs/app.js` 新增「会议 / 期刊」facet（venueBase 去年份聚合；≥3 篇直接展示、<3 篇折叠进"更多"details；arXiv（预印本）/其他/未标注 殿后），`style.css` 加 .facet-more 样式。filters 加 venue 维度。

## 十四轮："其他"130 篇拆分 + 默认折叠
- 用户："其他的130篇也拆分一下吧，默认折叠起来不显示。"
- `generate.py` 新增 `crossref_container_short(url)`：从缓存 Crossref `container-title` 推导真实小 venue 名（去"Proceedings of"、前导年份、卷号后缀；解码 `&amp;`/`&lt;`/`&gt;`/`&#38;`；>60 字截断）。DOI 论文改为 `crossref_container_short(url) or "其他"` 并标 `venue_minor=True`；SECONDARY_VENUES 也改 `venue_minor=True` 保留真名（不再叫"其他"）。
- `rec` 增加 `venue_minor`；`stats` 增加 `minor_venues`（去重真实小 venue 名列表）。
- README venue 分布：主 venue（非 minor 且 count≥2）直列；其余（minor + count<2）折成单行"其他" + 折叠 `<details>`「其他」明细表（每 venue 计数）。
- 网站 `docs/app.js`：`venue` 加入 `state.filters`；`venueBase(p)` 去年份聚合；`counts("venue")` 用 venueBase；`renderFacets` 新增「会议 / 期刊」分组（主 venue ≥3 直列、<3 或 minor 折进"更多"details）；`matches(p)` 加 venue 过滤；active chips 加 venue。
- 拆分暴露 2 篇离题论文 → 从 `raw_data.py` 删除，OFF_TOPIC 正则扩展（hallucination-like experiences / launay-slade / hallucination scale / stroboscopic / strobe stimulation）：
  - "Schizophrenia Research: Cognition"（Launay-Slade Hallucination Scale，心理）
  - "Journal of Vision"（Stroboscopic hallucination，感知科学）
- 修 bug：`crossref_container_short` 解码 `&amp;` 等 HTML 实体（如 "AI &amp; Applications" 原样泄漏）。
- 最终 generate：1058 篇；by_ccf {未收录:435, C:57, B:195, A:371}；by_venue 全真实名、无"其他"裸键；minor_venues ~79–80 个、128 篇标 venue_minor。
- 一致性验证：删 2 离题后 1060→1058；`data/papers.json` ≡ `docs/papers.json`（完全一致）；README "其他"=164/115 与按年份剥离后 fold 计数（115 venue / 164 篇）完全吻合；app.js `fetch("papers.json")` + `venueBase` 去年份。

### ⚠️ 重要不变量（易错）
- 存储的 `venue` 字段**保留届次年份后缀**（如 "ACL 2026"、"EMNLP 2025"），这是 per-edition 规范值；展示/聚合时由 `venue_label()`（generate.py）与 `venueBase()`（app.js）剥离年份。
- 因此"其他"折叠判定必须以**年份剥离后的 base** 计算，否则 "ICML 2026"(1) 会被误判为独立小 venue，与 README "ICML"(14) 重复/不一致。重算任何统计前务必先 vbase 剥离年份。

## 十五轮：补 CV（图像/视觉）模态标签
- 用户："模态标签怎么没加个cv的。" — 站点 `模态 / 领域标签` facet 只有 Video/Audio/3D + 场景(Multilingual/Medical/Agent/Relation/Attribute)，缺"CV"（图像/视觉）这一基础视觉模态。
- `generate.py` `extra_tags()` 新增 `CV`：触发 image/imagery/visual/vision/photo/scene/object/grounding/caption/ocr/diagram/chart/figure/pixel/region/spatial/computer vision 等视觉线索；与 Video/3D **互斥**（命中 Video/3D 则不标 CV，保持模态分桶不重叠）；并 **gate 在 `model_type != "LLM"`**（纯文本 LLM 的"caption/grounding/object"误触发已排除，初版有 12 篇误标，加 LLM 守卫后归零）。
- `write_readme` 分类体系说明追加 `CV`（图像/视觉模态）；`docs/app.js` `TAG_ORDER` 在 Attribute 后插入 "CV"。
- 重新 generate：CV=456（无 LLM 误标、CV∩Video=0、CV∩3D=0）；by_tag 现 10 类。data/docs papers.json 一致。
- 注意：CV 标签依赖 `model_type`（classify_model 结果），调用 `extra_tags(title, abstract, model_type)` 传入；若后续改 model_type 逻辑需同步。

## 十六轮：arXiv 预印本升级为正式会议/期刊
- 用户："把 arxiv 标注的再去各个顶会搜搜看看，尽量更新一下成为正式的会议或者期刊。"
- 现状：259 篇 venue=arXiv（预印本）（2026:169, 2025:74, 2024:14, 2023:2）。其中 251 篇在 `data/venue_links.json` 已有陈旧 `""` 负缓存（之前跑 `enrich_links.dblp_venues` 时尚未发表，现多为已发表）。
- 新建 `scripts/update_arxiv_venues.py`，两路信号 + 重试/退避（瞬时 5xx/429/超时 **绝不缓存为负**，只缓存"真·无结果"；增量写、可续跑）：
  - Pass A：arXiv `journal_ref`/`doi` 批量（id_list 分批）抓取 → 用 `_XREF_VENUE` 式模式解析规范 venue + 年份（仅识别到已知 venue 才赋值；Findings 只输出主 venue，见下）；
  - Pass B：DBLP 标题检索（项目金标准）→ 取非 CoRR 且标题相似度 ≥0.92 的命中，写 `{venue, year, ee}`。
  - generate.py 消费 `venue_links[key]`，`canon_venue(short_venue(...))` 规范化，与现有 682 条正式条目一致。
- ⚠️ 关键约束：`data/ccf.json` **无 Findings 键**（只有 "ACL" 没有 "ACL-Findings"）。故只输出主 venue（如 `EMNLP`），**绝不输出 `EMNLP (Findings)`**——否则 Findings 论文会被 `ccf_of` 误判为"未收录"。
- 实测：arXiv `journal_ref` 对这批 hallucination 论文大多为空（作者未回填），故 **DBLP 是主信号**；抽样 15 篇较老论文仅 1 篇命中（ACL 2024），说明不少仍为预印本（正确保留 arXiv）。169 篇 2026 预印本多数尚未发表→本就应是 arXiv；但已录用的 2026 会议(ICLR/ICML/CVPR/AAAI 2026)论文已在 DBLP→会被升级。
- 后台运行中（task Xmu9Wv），完成后 `generate.py` 重生成并核对：arXiv 数下降、正式 venue 上升、无异常 venue。

### 十六轮结果（后台 task Xmu9Wv 完成，29 分钟）
- 全量跑完 259 篇 arXiv：**本轮新升级 7 篇**（`Newly resolved this run: 7/259`）：
  - Pass A（arXiv journal_ref，作者自填，最权威，ee 为空但主链接仍是 arXiv）：ICML 2026「Grad Detect」、ICML 2026「Correcting Visual Blur…」、ECCV 2026「See Only When Needed」、ICLR 2026「AFTER…」、ICLR 2026「Token-Guard」、ACM MM 2025「Mitigating Image Captioning…」。
  - Pass B（DBLP，带权威 ee）：ACL 2026「Mitigating Hallucinations…without Performance Degradation」→ `aclanthology.org/2026.acl-long.89/`（非误配，标题相似度≥0.92 且有具体 anthology 链接）。
- 重生成后：`arXiv（预印本）` 259→**252**（−7，占比 23.8%）；by_ccf 由 {A:371,B:195,C:57,未收录:435} → **{A:377(+6),B:196(+1),C:57,未收录:428(−7)}**（ECCV 属 CCF-B 故 +1B，其余 6 篇 ICML/ICLR/ACM MM/ACL 均 CCF-A）。CV 标签 456 不变；总数 1058 不变。
- data/docs papers.json 完全一致（md5 相同）；README 246012 字节，arXiv 行=252/23.8%。
- 结论：其余 252 篇多为 2026 尚未正式发表的预印本（作者未回填 journal_ref、DBLP 仅 CoRR 记录），**正确保留 arXiv**；无需强升。累计已有 142 篇 arXiv-origin 论文挂正式 venue（跨轮沉淀）。
- 复用价值：`update_arxiv_venues.py` 可续跑，日后有新预印本被顶会录用时重跑即可增量升级。

## 十七轮：TNNLS / TASLP / TAI / PRCV 正常列出（不折叠 + 缩写合并）
- 用户："TNNLS 和 TASLP 可以正常列出来。TAI 这个也可以正常列出来。prcv 也是。" — 这四个知名 venue 之前被长名分裂/折进「其他」。
- 三处修改（generate.py + app.js）：
  1. `_XREF_VENUE` 增加 TNNLS、TAI 模式；TASLP 模式改为 `IEEE(/ACM)? Transactions on Audio`（**该刊 2025 年更名去掉了 "/ACM"**，新 DOI 前缀 `taslpro`，旧 `TASLP`）。Crossref 兜底路径由此直接解析为标准缩写、venue_minor=False。
  2. `JOURNAL_ABBR` 增加 "ieee acm trans. audio speech lang. process"→TASLP、"ieee trans. audio speech lang. process"→TASLP、"ieee trans. artif. intell"→TAI（DBLP 缩写规范化）。
  3. 折叠豁免白名单：generate.py `ALWAYS_LIST_VENUES = {"TNNLS","TASLP","TAI","PRCV"}`（README 主表豁免 count==1/minor 折叠规则）+ app.js `ALWAYS_LIST`（站点 facet 豁免 ≥3 篇门槛）。**两处需保持同步**。
- 结果：TASLP=4（3 新刊名 + 1 旧名合并）、TAI=3（2 长名 + 1 DBLP 缩写）、TNNLS=1、PRCV=2，全部进 README 主表和站点 facet 主列表，minor=False。
- CCF：**B 196→200（+4）**（TASLP×3 + TNNLS×1 由长名对不上 ccf.json 键 → 现正确评为 B）；未收录 428→424。TAI 不在 CCF 2022 目录、PRCV 在目录但无评级——两者保持未收录（属实，勿"修复"）。
- 「其他」明细 115→110 venue、164→156 篇。总数 1058 不变，data/docs papers.json md5 一致。

## 十八轮：补收 2019 年幻觉论文（+6 篇，总数 1058→1064）
- 用户："2019年的也可以搜集一下。" 库口径不限 MLLM——早期 NMT/NLG/摘要事实性幻觉研究也收（2018 Rohrbach、2021 NMT 幻觉皆是先例）。
- Web 检索 + Crossref/DBLP/arXiv API 逐篇验证后加入 `raw_data.py`（新增 2019 分节）：
  1. A Simple Recipe towards Reducing Hallucination in Neural Surface Realisation — Nie et al., **ACL 2019**（P19-1256，CCF-A）
  2. Sticking to the Facts: Confident Decoding for Faithful Data-to-Text Generation — Tian et al., **arXiv 1910.08684**（从未正式发表，正确保留 arXiv）
  3. Identifying Fluently Inadequate Output in Neural and Statistical MT — Martindale et al., **MTSummit 2019**（W19-6623）
  4. Domain Robustness in NMT — Müller et al., arXiv 1911.03109 → 正式发表 **AMTA 2020**（管线规则 venue 年份优先，归入 2020，2020 年 1→2 篇）
  5. Assessing The Factual Accuracy of Generated Text — Goodrich et al., **KDD 2019**（CCF-A）
  6. Ranking Generated Summaries by Correctness — Falke et al., **ACL 2019**（P19-1213，CCF-A）
- 元数据：两篇 arXiv 摘要写入 `abstracts.json`（393→395）；6 篇 venue 写入 `venue_links.json`（DBLP 3 篇 503，改用已验证的 Crossref/DBLP 结果手工写入）。
- 结果：by_year 2019=5、2020=2；by_ccf A 377→380（KDD+ACL×2）；总数 1064。data/docs papers.json md5 一致。一次性脚本 add_2019.py 已删。
- 经验：DBLP 503 时不要死等——若此前已通过 Crossref/浏览器验证过条目，直接手工写 venue_links.json 同格式记录即可。

## 十七轮：删 2 篇 2019 批次
- 用户要求删除「补收 2019」批次的第 4、6 篇：
  - 第4篇 Identifying Fluently Inadequate Output in NMT（Martindale, MT Summit 2019）→ raw_data.py 删除 + venue_links 删 MTSummit 记录
  - 第6篇 Domain Robustness in NMT（Müller, AMTA 2020）→ raw_data.py 删除 + venue_links 删 AMTA 记录 + abstracts 删 1911.03109
- 重生成：总数 1064→1062；2019=4、2020=1；CCF 不变（两篇均非 CCF，A/B/C 与删前一致 380/200/57）。data/docs papers.json md5 一致。

## 十八轮：站点中英可切换（i18n）
- 用户要求「做个中英文可切换的」。实现纯前端 i18n，论文数据（标题/作者/摘要）保持英文原样，仅 UI 文案翻译。
- 改动：docs/index.html（加 #langToggle 按钮 + 静态文案 `data-i18n` 标记，footer 改用 #footLine）、docs/app.js（新增 I18N{zh,en} 字典 43 键对称；state.lang 默认读 localStorage `hal-lang`；`T()` 取文案、`MDL/MTH` 取模型/方法本地化标签；`venueBase` 与 SPECIAL/活跃筛选项/统计/分面/表格表头全部走 T；`setLang` 切换并重渲染；右上角「EN/中」按钮一键切换并持久化）。
- 顺带修复：表格表头原本 9 个 th 但只有 8 列 td（多出一个「场景」），重排为 8 列（#/标题/作者/时间/模型/方法/会议期刊/代码）。
- 验证：node --check 通过；node 脚本校验 zh/en 各 43 键完全对称、HTML+JS 用到的 43 键全部齐备。沙箱网络限制下无法第二个 shell 连通本地 http.server（系统预览通道独立），以静态校验为准。

## 十九轮：整体体检 + 修 stale 年份范围 bug
- 体检发现 app.js 的 KPI 与页脚写死「2022–2026」，但库已含 2018/2019/2020 及 5 篇 2027（Springer LNCS 真实 forthcoming），README 已正确写「2018–2027」。
- 修复：新增 `yearRange()` 从 `state.stats.by_year` 动态算 min–max，I18N 的 withLink/footerTotal 改为 `(n, yr)=>...` 并传入；node --check 通过，确认无残留硬编码。
- 体检数据：1062 篇 / 1060 有链接；缺 abstract 667(62%，多为非 arXiv 正式 venue 论文)、缺 code 971(91%)、缺 venue_url 434(40%，其中 181 篇有正式 venue 却无官方链接)；175 篇(16%)仅年份无月；5 篇 2027、2 篇无链接(RAR/HAIT，venue 未标注)；无重复标题；10 个标签全覆盖。style.css 干净，仅 `.p-summary` 与 `.b-obj/.b-attr/.b-rel/.b-gen` 为未使用死样式。
- 优化建议（已向用户汇报，待选）：①分页/虚拟滚动（1062 卡一次性渲染）②搜索防抖 ③筛选状态写入 URL hash 可分享 ④CSV 导出筛选结果 ⑤摘要/venue_url/code 链接富集（大工程）⑥清死 CSS。

## 十七轮：整体优化落地（用户批复「都逐步做，自己决定」）
- **① 分页**：`app.js` 增 `state.page/pageSize`，默认 100，下拉 50/100/200/500；`render()` 切片当前页；`renderPager()` 窗口化页码（首/尾+当前±2+省略号）+ 上一页/下一页 + `pageInfo` 文案；切筛选自动重置到第 1 页。新增 `#pager` 容器与 `style.css .pager/*` 样式。
- **② 搜索防抖**：搜索输入 150ms `setTimeout` 防抖，避免每次按键全量重渲染。
- **③ URL hash**：`buildHash()/applyHashFromLocation()` 把 view/sort/lang/page/pageSize/q/model/method/year/ccf/tags/venue 编码进 `#`，用 `history.replaceState` 写入（不产生历史记录、不触发 hashchange 死循环）；`init()` 先 `applyHashFromLocation()` 还原，`syncControlsFromState()` 同步控件值。分享链接可一键复现筛选。
- **④ CSV 导出**：`#exportBtn` 调 `exportCsv()`，导出当前筛选+排序结果（#/标题/作者/年份/venue/CCF/模型/方法/标签/code/url/官方），带 BOM 防 Excel 乱码，正确转义逗号/引号/换行。
- **⑥ 清死 CSS**：删 `.p-summary` 与 `.b-obj/.b-attr/.b-rel/.b-gen` 四条未用规则。
- i18n 增 `pageSizeLabel/exportCsv/prev/next/pageInfo`（中/英），zh/en 各 48 键完全对称。node --check 通过；node 脚本校验键对称、DOM id 齐备、hash 往返（含带空格 venue「ACL 2024」）、CSV 转义均正确。
- **⑤ 数据富集（分两步）**：
  - 安全回填 `venue_url`：在 `generate.py` 主循环加规则——`venue_url` 为空且 venue≠arXiv 且 url 为官方出版方域名(doi.org/aclanthology/openreview/thecvf/ieee/acm/springer/mdpi/nature/sciencedirect)时，用 `url` 作 `venue_url`。重生成后 with_venue 639→**788**（+149，均为 DOI 论文）。
  - 摘要富集：`scripts/enrich_abstracts.py`（新建，可续跑、限流重试、绝不误缓存瞬态错误）查 Crossref 给「无摘要且 url 含 doi」的论文补摘要，写入 `data/abstracts_extra.json`（按 norm(title) 键）；`generate.py` 主循环已接 `abstracts_title = load_json(abstracts_extra.json)` 并在 arxiv 摘要缺失时回退到该表。
  - 摘要富集任务 7nTr70 已完成：488 候选跑完，**新补 84 篇摘要**（Crossref 无摘要的 ACL/EMNLP/CVPR 等正确跳过）。重跑 `generate.py` 并入后：`with_abstract` 395→**479**（覆盖 37%→45%）、LaTeX 残留 0、`venue_url` 788（74%）。`data/papers.json` ≡ `docs/papers.json`（md5 一致），README 已重生成（年份 2018–2027、摘要 479/1062）。任务 17 完成，全部 6 项优化交付。

## 二十轮：第二轮整体优化（前端 4 项 + 数据缺口审计）
- 用户："整体检查还有啥优化的" → 通读 app.js + 探数据，再落地。
- 🐞 主题持久化：新增 `applyTheme/restoreTheme`，切暗亮写入 localStorage「hal-theme」并在 init 恢复（此前刷新丢失，默认 dark）。
- 🐞 活跃筛选条 ✕ 删除时补 `pushHash()`，与 toggleFilter 行为一致（此前不更新 URL hash）。
- 💡 翻页回顶：`scrollToResults()` 在 pager 点击时 `results.scrollIntoView({behavior:"smooth"})`。
- 💡 分面计数随筛选变化：`matches`→`matchesExcept(p,exceptDim)`，`counts(dim,exceptDim)` 计算时排除自身维度，renderFacets 各维度传自身为 exceptDim。逻辑经独立 node 测试验证（全局/子集/搜索三种场景正确）。
- 数据缺口审计：code 链接仅 91/1062（8.6%）为最大剩余缺口；探测 **Papers-with-Code API 已停用**（返回 HTML 落地页非 JSON），GitHub 搜索限流且标题匹配不可靠 → 不假做自动补全，如实标注。2027 论文 5 篇均为 Springer LNCS/LNNS 真实书章（venue 为丛书名、ccf 空），保留。venue="其他" 论文数 0，`others` 分面分支为无害死代码。node --check 通过，纯前端改动无需重生成 papers.json。

## 二十一轮：代码链接爬虫简化（用户拍板"official + 标题"）
- 用户原想做多源复杂爬取（S2/Anthology/GitHub/OpenReview 各自脚本），探明：S2 `code` 字段现需 `S2_API_KEY` 且未带 key 限流(429/404)；Anthology `software` 字段极稀疏；GitHub 盲搜很噪。用户最终："算了，代码没有就算了，简单的搜 official + 标题。"
- **方案定稿（简单）**：删除 4 个多源脚本 + 旧 fetch_code 合并脚本，仅留 `scripts/fetch_code.py`（单 GitHub 搜索 `official <title>`，兜底 `<title>`；接受首个非 awesome/survey/list 类仓库；找到即写 data/code_links.json，找不到跳过）。
- 代码：`scripts/code_common.py`(norm_title/load/save/http_get 带 403/429/503 退避) + `scripts/fetch_code.py`。已用 mock 验证：LLaVA 等缩写名仓库可被接受、awesome-list 被正确跳过。
- ⚠️ 实测坑：未带 `GITHUB_TOKEN` 时 GitHub 搜索仅 60 次/小时，连续测试后 403 导致静默跳过（表现为 0 命中）。全量跑须设 `GITHUB_TOKEN`（5000/hr）或错峰。生成用 `python scripts/generate.py`。
- 用户偏好：代码链接不强求补全，简单搜不到就留空。

## 二十二轮：剩余摘要按对应网站爬取（fetch_abstracts_web.py）
- 用户："剩余的摘要可以自己去对应的网站尝试爬取一下。" → 不再只靠 Crossref（已 479/1062=45%）。
- 缺口分桶（583 篇无摘要）：ACL-Anthology 260(含 SemEval 20)、CVF 50、NeurIPS 26、OpenReview 21、ICASSP 20、IEEE 15、ACM 16、未标注 15、其余长尾各 1-5 篇。
- 新建 `scripts/fetch_abstracts_web.py`：按论文 venue/url 路由到对应站点抓 abstract，写入 `data/abstracts_extra.json`（generate.py 第 374/399 行读取的 norm_title→abstract 表，与 arXiv `abstracts.json` 互补）。可续跑、对 403 容错。
  - Anthology：HTML `id=paperAbstract` 区块（⚠️ Anthology 用**无引号属性** `id=paperAbstract`，正则须引号无关；且可用 DOI `10.18653/v1/<id>` 反推 Anthology ID）。
  - CVF：`openaccess.thecvf.com` 的 `<div id="abstract">`（已验证命中）。
  - NeurIPS：`papers.nips.cc/.../hash-Abstract-Conference.html` 的 `id=abstract`。
  - OpenReview：`api.openreview.net/notes?id=<id>` 的 `content.abstract`。
  - arXiv：仍缺的 arxiv id 走 export.arxiv.org API。
- 关键坑：本 session 连测多个 API 把出口 IP 限流，Anthology/NeurIPS/OpenReview 间歇性 403、页面结构时变；故 `code_common.http_get` 的 403/429/503 改为**快速失败**（最多 2 次重试、短退避即放弃），避免全量爬取卡死。CVF/Anthology(2026) 实测可稳定抓取。
- `--limit 25` 验证：+17 条 ACL 摘要（84→101）。全量已在后台跑（task DXmZZM，日志 data/_abs_crawl.log），跑完需 `python scripts/generate.py` 重生 README/docs 并统计新覆盖率。
- 用户偏好（补充）：摘要缺的可去各会议/出版方官网直接爬。
- 用户要求"略微整理列一下幻觉综述文章"。从 papers.json 用 title 关键词（survey/review/taxonomy/overview）筛出 15 篇（2024–2026），按 5 类（通用LLM/多模态视频/评测事实核查/推理解码缓解/领域特定）归类，产出 `SURVEYS.md`（含标题官方链接+CCF评级）。这些是 1062 篇库子集。
- 整体正确性审计：新建 `scripts/audit.py`（对比 README 声明 vs papers.json 真实值 + 硬不变量）。结论：data≡docs md5 一致、CCF 与 ccf.json 0 处不符、各项统计(总数1062/链接1060/摘要479/代码91/CCF A380 B200 C57 未收录425/年份2018–2027/Benchmark97/重复0/字段完整)全部与 papers.json 一致。修复 README 分类体系表写死的"年份 2022–2026"→"2018–2027"（其余 README 数字本就正确）。另发现小瑕疵：1 篇标题 LaTeX 残留 `P$^2$-DPO`（已改源 raw_data.py:234 → `P²-DPO`），2 篇无链接论文(RAR/HAIT，源里 url 为空，arXiv 限流待补)。abstracts 全量爬取仍在后台(DXmZZM)，完成后需 `python scripts/generate.py` 重生以刷新摘要数并固化上述修复。

- Survey/Benchmark 拆分（修正误标）：generate.py 将 EVAL_STRONG（含 `\bsurvey\b` 导致所有含 survey 的论文被误标 Benchmark）拆为 BENCH_STRONG（基准/数据集）与 SURVEY_STRONG（综述），新增 `is_benchmark`/`is_survey`（标题主导，避免 related-work 噪声）；stats 加 `surveys`；README 拆「📋 基准(91)」+「📚 综述(15)」两节；app.js `TAG_ORDER` 加 Survey、i18n `surveyTag`、badge 分色（`.b-survey` 紫）；style.css 加 `--c-survey`。重生成后 Benchmark 91 / Survey 15（原 Benchmark 97 含 survey 误标已修正），with_abstract 561（爬取 +166 条）。
- 摘要全量爬取 DXmZZM 已停（限流），`data/abstracts_extra.json` 166 条落盘。
- 用户取消全部代码爬取任务(#18–#24)。`fetch_code.py` 保留不维护（独立官方+标题搜索脚本，不参与生成管线）；`code_common.py` 因被 `fetch_abstracts_web.py` 复用保留。

- Benchmark 误标大修（用户反馈"很多不是 benchmark"）：`generate.py` 的 `is_benchmark` 原靠宽松摘要分支 BENCH_ABSTRACT 命中，导致 91 篇里 48 篇混入方法/分析/检测论文。改为**标题主导** `BENCH_TITLE`（benchmark|bench|dataset|testbed|leaderboard + XBench 命名），仅保留 3 个真 benchmark 白名单(CodeHalu/KG-FPQ/NOPE 标题无标准词)。Benchmark 91→**58**（全部真基准/数据集）。`is_survey` 与 `is_benchmark` 在构建循环互斥（Survey 优先），修复 Siren's Song 被双标。
- method_type 审计（用户要求再检查）：Training-based 93 篇中 75 篇真训练缓解(DPO/RL/微调)，质量高；真正问题在"默认 Training-free 漏标"——969 free 里 57 篇含 dpo/rlhf/finetune 信号却因默认归 free 被判错(PretrainRL 等)。本轮**未改代码**（避免引入新误标），需在下一轮确认是否扩展 TRAINING_BASED_SIGNALS。
- 自动更新落地：新建 `scripts/fetch_new_arxiv.py`（arXiv API 抓 hallucination 预印本、按 norm_title 去重、写 data/incremental.json 并同步摘要到 data/abstracts.json，时间窗 RECENT_DAYS=14）。`generate.py` 构建时合并 incremental.json。automation_update 创建每周一自动更新任务(automation-1784902350808)：fetch→generate→汇报新增/总数/摘要数。首次跑抓到 124 篇 backlog 合并，库 1062→**1186**，摘要 561→685。回退方式：删 data/incremental.json 后重跑 generate.py（已备 data/papers.json.pre_incremental 备份）。

## 二十三轮：训练信号修正 57 漏标 + arXiv 更新节奏重构
- **arXiv 更新规则核查**：arXiv API（export.arxiv.org）开放无 key；官方礼貌建议约 1 请求/3 秒、单次响应 max_results≤2000；标准做法是**每日 scheduled-diff**（按 id 去重）。故可安全缩短轮询间隔。
- **训练信号扩展（用户拍板"①扩展训练信号修正那 57 篇漏标"）**：
  - `generate.py` 重写 `TRAINING_BASED_SIGNALS` 为**施事性/高精确**模式（去掉会误伤的 "training data"/"fine-tuning on"/"fine-tuned LLMs"/"fine-tuning a|an"/裸 `\bsft\b"` 等宽松分支）；新增 `METHOD_TRAINING_ALLOW`（28 个手验 norm_title 白名单，强制 Training-based）。
  - `classify_method` 改为：白名单优先 → TRAINING_FREE 胜出 → TRAINING_BASED 命中 → 默认 free。
  - 结果：Training-based 93→**137**（+44 = 28 白名单 + 5 正则新增命中 − 12 篇旧宽松正则误标为 based 的论文（Self-Consistency/Contrastive Decoding/各 Benchmark/分析类，仅因"training data"被误判）纠正回 free）。抽查确认 PretrainRL/DA-DPO/Luna/WeedExpert-R1/GHOST(图像生成版)/Can Knowledge Editing/MoSEAR(PEFT) 等均正确变 based；PROBE/Self-Consistency/LLMs-With-Contrastive-Decoding 保持 free。
  - 关键：正则收窄既修"漏标 free"也修"误标 based"，双向提升精度；白名单保证 28 篇确定性正确。
- **自动化重构（"每两个月大更新一次；平常只需要更新 arxiv"）**：
  - 现有任务 automation-1784902350808 由「每周」改 **每 2 天**（`FREQ=DAILY;INTERVAL=2`，仅 arXiv + generate，已 arxiv-only）。
  - 新建「大更新」任务 automation-1784903054118：**每 2 个月**（`FREQ=MONTHLY;INTERVAL=2;BYMONTHDAY=1`），步骤 = fetch_new_arxiv + fetch_abstracts_web(补缺失摘要) + generate + 汇报；明确"会议论文仍需手动补 raw_data.py，不自动合并未审核候选"。
- 🐞 **修 incremental.json 覆盖 bug**：`fetch_new_arxiv.py` 原每次用最近 21 天窗口**覆盖**写 incremental.json，导致早于该窗口、上轮已收录的论文在下轮 generate 时丢失。改为**累积合并**（按 norm_title 去重），与 abstracts.json 累积逻辑一致。附：RECENT_DAYS 由 14 调 21（频繁跑更安全）。
- 校验：`audit.py` 通过（data≡docs md5 一致、CCF 0 不符、无重复、字段完整）；临时诊断脚本 `_diag_method*.py` 已删。最终 1186 篇 / 摘要 685 / Benchmark 64 / Survey 16 / based 137。

## 二十四轮：arXiv 历史回填（标题命中，2024-01-01 起）
- 用户："把前面更新的这两周的再往前扩展一些，你看看应该从哪些时间补一下。" → 决定历史补抓范围。
- 探查现状：`fetch_new_arxiv.py` 仅 `RECENT_DAYS=21` + 取最新 200 条（无分页），故此前 harvest 只抓到 2026-07 的 124 篇；库内 arXiv 519 篇年份分布 2019:1/2023:4/2024:30/2025:133/2026:351 —— 2024–2025 靠手动 raw_data.py 覆盖极稀疏，是真实缺口。
- 给 `fetch_new_arxiv.py` 加**分页 + 日期下限** backfill 能力：`harvest(start, from_date, to_date)` 支持 `submittedDate:[下限 TO 上限]` 过滤 + `start` 分页（MAX_PAGES=200、页间 sleep 3s 礼貌）；新增 `--from/--to/--count/--ti` 参数。`--count` 干跑不写盘；`--ti` 用 `ti:hallucination` 标题命中（高精度）。
- **精度决策（关键）**：宽查询 `all:hallucination` 从 2024→now 干跑得 5061 篇新候选，但抽样显示仅 ~23% 标题含 "hallucination"（其余是图像超分/推荐/会员推断等无关主题，因词出现在摘要/脚注被误抓）——全量合并会污染精选库。改用**标题命中 `ti:hallucination`**，干跑得 835 篇（2024:213/2025:367/2026:255），全部为标题即"幻觉"主题，精度高、规模合理。
- 选定下限 **2024-01-01**（2023 及更早手动库已较完整；如需可再 `--from 20230101 --ti` 补）。执行真实合并：incremental.json 124→**959**（+835）；`generate.py` 重生成：库 1186→**2021**。
- 结果：by_year 2024:393/2025:766/2026:821（2024–2025 缺口补齐）；arXiv（预印本）1208；Benchmark 117(+53)、Survey 33(+17)、based 275(+138)、摘要 1520(+835 全有摘要)。`audit.py` 通过（data≡docs md5 一致、CCF 0 不符、无重复、字段完整）；抽查 CONFIDE/BHRAM-IL/HalluGraph 等确认主题相关且分类正确；临时 `_diag_backfill.py` 已删。
- 注意：2-day 自动化(automation-1784902350808) 与 2-month 大更新(automation-1784903054118) 仍用 broad `all:hallucination` 抓**最新**（低频、volume 小、recall 高）；本次 `--ti` 高精度回填为一次性历史补抓，二者不冲突。

## 二十五轮：去重缺陷修复 + 调度逻辑加固（用户要求整体查逻辑）
- 用户："整体上检查一下逻辑有没有问题…arxiv要搜一下更新时间和调休的时间，比如好像周末两天不更新你也记得跳过，或者大更新的时间放到周末。以及没去重吗。重点检查新增加的对不对。"
- **发现 backfill 去重缺陷（你担心的"没去重"）**：`fetch_new_arxiv.py` 的 `parse_entry` 未对 arXiv 返回标题做 HTML 实体解码（`&quot;`/`&apos;`/`&amp;`）与 Unicode 归一化，归一化键被污染（`&quot;` 去标点后残留 `quot` 四字母）→ 同一篇论文被当新论文重复加入。诊断：精确 norm_title 重复 0（假象），但 **arXiv ID 碰撞 8 组、近重复 17 组**，多为编码/实体差异（`P$^2$`vs`P²`、`Schro`vs`Schrö`、ReLoop`&quot;`、Benford`&apos;` 等）。
- **根因修复**（`fetch_new_arxiv.py`）：`parse_entry` 加 `_clean_text`（html.unescape + NFKC）；新增 `fnorm`（NFKD + 去组合符，ö→o、²→2、全角→半角）；`load_existing` 同时返回已有 **arXiv ID 集合**；`process` 对"同 arXiv ID（=同论文）"或"折叠后标题相同"直接跳过；`merge_incremental` 存储键改用 fnorm 保持前后一致。今后 backfill 不会再因实体/变音符号漏去重。
- **一次性清理**：`_clean_incremental.py` 从 incremental.json 移除与库中已有论文重复的 backfill 条目（同 ID 或折叠后标题相同），并对剩余条目标题/摘要做实体清洗。共移除 **15 条**，库 2021→**2006**。
- **剩余重复均为 raw_data.py 手工旧问题（非 backfill）**：arXiv ID 碰撞仅剩 2 组——`2501.01926`（两篇不同论文误共享 ID，一条 URL 标错）、`2505.10634`（同一论文改名被录两次）；近重复 5 组亦为 raw_data 旧条目（SemEval-2024 Task6 / HaDeMiF 等）。已向用户说明，待其在 raw_data.py 手动修（超出本轮 backfill 范围）。
- `audit.py` 通过（data≡docs md5 一致、CCF 0 不符、无缺字段、重复标题 0）。临时诊断/清理脚本已删。
- **调度加固（周末/调休）**：日常 arXiv 任务 `automation-1784902350808` 由"每2天"改 **周一/三/五**（`FREQ=WEEKLY;INTERVAL=1;BYDAY=MO,WE,FR`，跳过周末）；大更新 `automation-1784903054118` 由"每月1号"改 **约每2个月·周六**（`FREQ=WEEKLY;INTERVAL=8;BYDAY=SA`；原 `MONTHLY;INTERVAL=2;BYDAY=SA;BYSETPOS=1` 被校验器拒，因 MONTHLY 必须带 BYMONTHDAY）。说明：arXiv 每日出新、跳过周末下次补齐不漏数据；精确"调休"（周六变工作日/周二变假日）RRULE 无法原生表达，如需可在脚本加节假日跳过守卫（需硬编码节假日表），待用户定。

## 二十六轮：主题相关性审查（用户质疑"过了之前的清理规则吗"）→ 修复 + 全量清理存量
- 用户："这些爬取出来的过了之前的清理规则吗，比如是一些经典的cv任务，只是标题提到了这个关键词，但是实际并不是幻觉任务。"
- **诊断结论**：之前"清理规则"只有分类+去重层面（标题含 hallucination→ti 命中、去重、method_type/benchmark/survey 分类），**无主题相关性过滤**。两类脏数据：
  1. **BROAD 组 117 篇（最严重）**：标题根本不含 hallucination，是早期自动化用 broad `all:hallucination` 查询被"共现词"带进来的（incremental.json 残留最初 124 篇 broad 抓取）。内含大量明显无关：金融交易(EVOQUANT/AI Trading/TriAgent)/推荐系统/广告竞价(AIGB-R1)/希腊陶罐博物馆(VaseMuseum)/医疗影像重建超分(CT/MRI/CBCT)/CV视频生成编辑/语音合成/政治模拟等。
  2. **TI 组约 6 篇纯 CV 假**：标题含 hallucination 但用 CV 术语义（非本库忠实性主题）——Hallucination Score for Generative Image Super-Resolution、WonderHuman(3D人体重建补全)、DiffMAC(盲人脸修复)、Photorealistic Phantom Roads(3D自动驾驶)、sFRC(医学图像修复)、HalluGen(图像修复合成)。
- **根因修复**（`fetch_new_arxiv.py`）：默认查询 `ARXIV_FIELD` 由 `"all"` 改 `"ti"`（标题命中），broad 改为需显式 `--broad` 才用。今后日常(周一三五)/大更新(每8周六)自动化都只收标题含 hallucination 的预印本，杜绝经典 CV 任务靠共现词混入。
- **全量清理存量**（用户拍板"全量移除"）：新增 `_clean_broad.py`（预览+`--apply`）：从 incremental.json 移除 BROAD 组 117 篇（标题不含 hallucination）+ TI_CV 6 篇（纯 CV 超分/重建术语）。incremental 944→**821**（−123）；`generate.py` 重生成：库 2006→**1883**。
- ⚠️ 全量移除 BROAD 组也把其中少数真相关的（HALLMARK 引用验证、Groc-PO 忠实多模态、MissingBench 对象缺失、Faithful by Design、ArtChart、Do Medical VLMs Actually See、Deceptive Grounding、ProCap、HoloCount、WeedExpert-R1 等）一并删了。选项已说明"真相关的可手动加回 raw_data.py"——如需保留，下一轮从 incremental 恢复或手动加回 raw_data.py。
- `audit.py` 通过（字段完整、重复标题 0、data≡docs md5 一致；注 audit.py 自身"Benchmark 标记:0"为旧显示口径 bug，真实 Benchmark 111/Survey 32 见 generate stats）。最终 1883 篇 / 摘要 1382 / Benchmark 111 / Survey 32 / based 242 / arXiv 1070。临时脚本已删。

## 二十七轮：复用 collect_candidates.py 的 OFF_TOPIC 过滤规则（用户："我之前别的脚本里面应该有一些过滤规则"）
- **找到真正的"清理规则"**：`scripts/collect_candidates.py` 里定义的 `OFF_TOPIC` 正则（+ `relevant()` 函数 + `merge_candidates.py` 调用它做硬门禁），正是用户记忆中的过滤规则——专门剔除"hallucination"被用在**无关语境**的论文：医疗/精神科幻觉、CV 超分/重建/生成（face/texture hallucination、super-resolution、data/feature/depth hallucination、扩散/重建/合成、光流/立体/LiDAR/点云/高斯溅射/幻觉特征生成器等）、视觉错觉/偏头痛、哲学/心理学量表。而 backfill 的 `fetch_new_arxiv.py` **完全没套这个 OFF_TOPIC**，只过了"标题含词"硬门禁 → 脏数据钻空子。
- **诊断**：当前 `incremental.json`（821 条 TI 组）有 **16 条命中 `OFF_TOPIC`**，且全已在主库（说明污染 100% 来自 backfill；`raw_data.py` 手工库本身 0 命中、是干净的）。
- **关键陷阱（不能盲跑 OFF_TOPIC）**：16 条里约一半是 `OFF_TOPIC` 正则的**误杀(false positive)**——它们是正经 LLM/VLM 论文，只是标题凑巧用了 `clinical`/`psychosis`/`scale`/`cross-domain`/`prior` 等词（如 Detecting Clinical Hallucinations **in LVLMs**、System Hallucination Scale **(LLMs)**、DPO Suppressing **Hallucinated Prior Exams**(放射报告)、SpikeScore **Cross-Domain Hallucination Detection**、Chemical CoT Scratchpad、Purposefully Induced Psychosis **(LLMs)**）。盲跑会删掉好论文 → 必须人工按标题精确剔除。
- **修复（抓取脚本固化规则）**（`fetch_new_arxiv.py`）：引入 `from collect_candidates import OFF_TOPIC` + 新增 `_AI_MARKER` 护栏（含 llm/mllm/lvlms/vlm/gpt/large language model/multimodal/vision-language/chain-of-thought，补强后覆盖带前缀的 `LVLMs`/`MLLMs`），定义 `is_offtopic(title)`：OFF_TOPIC 命中且**无** AI 模型标记才判离题。`process()` 在去重后加 `is_offtopic` 跳过 + 计数打印。今后 backfill/日常都不会再引入题外 CV/心理论文（最多是某篇好论文没被自动加进来——召回损失，不 corruption）。
- **清理存量（精确白名单剔除 10 篇真·题外）**：`_clean_offtopic.py` 用 `code_common.norm_title` 精确匹配 10 篇 genuine off-topic 标题并移除，保留 6 篇 LLM/VLM 误杀项。移除清单：GHOST(Opaque Surface Textures)/SynMind(fMRI Reconstruction)/DreamPose3D(3D Pose)/Feature Hallucination(Action Recognition)/Hyperbolic State Space(域泛化)/LiDAR-Event Stereo/Anchor Hallucination(噪声标签)/Dual-View Data Hallucination(少样本)/Video Highlight Detection/AI Psychosis(人类心理)。incremental 821→**811**（−10）；`generate.py` 重生成：库 1883→**1873**。
- **核验**：6 篇应保留论文全部仍在库（子串确认）；10 篇题外全部 gone；`data/papers.json` ≡ `docs/papers.json`（md5 一致）；`audit.py` 通过（字段完整、重复标题 0、年份 2018–2027）。最终 1873 篇 / 摘要 1372 / Benchmark 111 / Survey 32 / based 241 / arXiv 1064。临时脚本已删。
- **经验**：`OFF_TOPIC` 是候选收集阶段（无上下文）的启发式，直接套精选库会误杀"标题含 LLM/VLM 关键词但碰巧带 clinical/psychosis/scale 等词"的论文；安全做法是"抓取时套 OFF_TOPIC+AI 标记护栏（仅防未来污染）+ 存量人工精确剔除"，而非盲跑正则。

## 二十八轮：会议/期刊收录(venue+CCF)更新 + 管线顺序审查
- 用户："这些新加的也得更新会议期刊收录情况。" → 指 backfill 进来的 ~820 篇 arXiv 预印本 + 全部 arXiv 论文都只填了 `venue="arXiv（预印本）"`、`ccf=""`，**从未做过 venue/CCF 解析**（库里那 127 篇"有 venue 的 arXiv 论文"是靠 DOI→Crossref 路径解析的已发表论文，不算预印本）。
- **正主工具**：`scripts/update_arxiv_venues.py`（用户工程里早有）。机制：Pass A 用 arXiv API 批量查 `journal_ref`/`doi`（权威、快，已确认 1064 篇预印本里有 **53 篇**已被正式录用并记录出处）；Pass B 对未录用的做 DBLP 标题检索补全；结果写入 `data/venue_links.json`（按 norm_title），`generate.py` 随后自动映射正式 venue + CCF 等级（ccf.json 已覆盖 ACL/EMNLP/CVPR/NeurIPS/TPAMI/IJCV 等）。读取 `docs/papers.json` 作输入。
- **已启动全量后台运行**（`update_arxiv_venues.py`，无 limit），task_id `gSUiTP`：Pass A 批量(~2.5min) + Pass B 对 ~1011 篇未录用预印本做 DBLP（2s 限速，全量约 40–55min）。完成后需 `generate.py` 重生成 + `audit.py` 校验。background 进程已加载脚本到内存，编辑脚本文件不影响在跑进程。
- **用户追问"还有啥优化角度、整体顺序是否不妥"** → 发现并修复关键顺序硬伤：两个自动化(daily `automation-1784902350808` / big `automation-1784903054118`)**都漏了 enrich(venue) 步骤**，且 `update_arxiv_venues.py` 读 `docs/papers.json`(generate 产物) → 必须"先 generate 物化→再 enrich→再 generate 应用"的两段依赖，自动化当初被此搞晕而跳过。**已修**：两自动化都改为 `fetch → generate(物化) → update_arxiv_venues(venue) → generate(应用)` 四步；enrich 写 venue_links.json（累积式、只对新/未缓存条目发请求，日常快）。
- **其余优化角度（已向用户列出，待其拍板）**：①扩充 ccf.json 覆盖(Information Fusion/KBS/ESWA/PR Letters/Workshop 等常见 ML venue，避免落"未收录")；②修 raw_data.py 遗留重复(2 组 arXiv ID 碰撞 + 5 组近重复，二十六轮已发现)；③update_arxiv_venues 健壮性——DBLP 瞬时 503 耗尽重试后缓存 `""`(伪未发表)，应改为"瞬时失败不写负缓存"；④audit.py 增强(venue 新鲜度/raw_data 重复/已解析 venue 与 ccf.json 一致性)；⑤incremental.json 裁剪(已毕业条目)；⑥根治建议——合成单一 `update_pipeline.py` 编排脚本(fetch→generate→enrich→generate→audit)消除顺序脆弱性。
- **待办**：gSUiTP 完成后自动重生成+审计，给用户 CCF-A/B/C/未收录分布汇报；并视用户选择做 ccf.json 扩充 / raw_data 重复修复 / update_arxiv_venues 负缓存健壮性。

## 二十九轮：用户全选 4 项优化→B/C/D 已完成，A 与最终校验待 gSUiTP
- 用户用 AskUserQuestion 全选了 4 项：扩充 ccf.json / 修 raw_data.py 重复 / enrich 负缓存健壮性 / 合成 update_pipeline.py。
- **B 修 raw_data.py 重复（已完成）**：写 `_diag_raw_dup.py` 精确诊断 raw_data.py 的 RAW 列表。结论：精确标题重复 0 组；共享 arXiv ID 仅 **1 组** `2505.10634`（line 481 与 509，同一篇《Cross-Image Contrastive Decoding》两标题版本）→ 删 line 481 保留 509。另发现 `2501.01926` 跨源碰撞：raw_data 两处——line 479《Mitigating Hallucination for LVLM by Inter-Modality Correlation Calibration Decoding》(标 2501.01926) 与 line 873《Cross-Modal Attention Calibration for LVLM Hallucination Mitigation》(url 空)。查 arXiv API 确认 **2501.01926 真实主人是 line 873**（2025-01-03 发表），line 479 的 2501.01926 是**错的**（其真实 ID 在 arXiv 无精确匹配）。修法：line 873 补 `https://arxiv.org/abs/2501.01926`；line 479 清空错误 url 为 `""`。重跑诊断确认 raw_data 共享 ID=0、精确重复=0（RAW 1062→1061）。注意：SemEval-2024 Task6 那 20+ 条是不同团队投稿、非重复，勿误删。
- **C enrich 负缓存健壮性（已完成）**：改 `update_arxiv_venues.py`——DBLP 瞬时失败(TransientError 耗尽重试)不再缓存 `""`，改设 `transient=True` 留键不写（下次自愈）；且已有 `journal_ref`/`doi` 但 VENUE_MATCH 未识别的也不写 `""`(留待未来扩展)。新增 `has_ref` 标志。py_compile 通过。注意：gSUiTP 后台进程已加载旧脚本，本修改只影响未来运行。
- **D 合成 update_pipeline.py（已完成）**：新建编排脚本，固化正确顺序 `fetch_new_arxiv → [fetch_abstracts_web] → generate(物化) → update_arxiv_venues → generate(应用) → audit`；支持 `--skip-fetch/--with-abstracts/--no-audit/--backfill/--from/--limit`。py_compile 通过。**未执行测试**（避免与 gSUiTP 并发写 venue_links.json）。后续应把两个自动化改为只调 `update_pipeline.py`（更稳）。
- **A 扩充 ccf.json（待 gSUiTP 后）**：需等 gSUiTP 完成、重生成后看 by_venue/by_ccf，把"已解析 venue 但落未收录且确属 CCF-2022 评级"的条目补进 ccf.json（仅补真 CCF 评级，不把非 CCF 期刊错标 A/B/C）。
- **最终校验（Task #37，待 gSUiTP）**：gSUiTP 完成后跑 `update_pipeline.py --with-abstracts`（或手动 generate→enrich→generate→audit），汇报 CCF-A/B/C/未收录分布、含摘要数、库总数；并把两自动化改为调 update_pipeline.py。
- **当前状态**：gSUiTP 后台运行中（Pass A 批量+Pass B DBLP ~40-55min），尚未完成。B/C/D 源改动已完成，待 gSUiTP 后统一重生成验证。
