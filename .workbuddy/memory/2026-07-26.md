# 2026-07-26 工作记录

## 项目概览
- 项目：`D:\downloads\hallucination` —— 幻觉研究交互资源库（MLLM/LLM 幻觉论文库）。
- 结构：Python 流水线（`scripts/`）采集/标注 → 生成 `data/papers.json` + `docs/papers.json`（GitHub Pages）+ `README.md`；前端 `docs/index.html` + `app.js`（中英双语、筛选、CSV 导出、URL hash 共享）。
- 当前规模：1892 篇（README 旧文写的 1062 已过时）。Benchmark 114 篇、Survey 若干；CCF-A 405 / B 218 / C 67 / 未收录 1202。

## 本次优化检查（已修复）
1. `audit.py` 用 `p.get("evaluation")` 统计 Benchmark，但字段名实为 `benchmark` → 审计恒为 0。已改为 `benchmark`，现正确显示 114。
2. `generate.py` 第 535 行 `except` 用 `sys.stderr` 但未 `import sys` → incremental.json 损坏时会抛 NameError 崩溃。已补 `import sys`。
3. 仓库无 `.gitignore` → 新建，忽略 `__pycache__`、2MB `abstracts.json`、各类备份/临时文件（`incremental.json.bak`、`papers.json.pre_incremental`、`_surveys_tmp.json` 等）。

## 待办优化（未做，已汇报用户）
- 高优：`venue_links.json` 有 67 条孤儿记录（指向已删/改标题论文），需重跑 `update_arxiv_venues.py` 对齐并清孤儿。
- 高优：`raw_data.py` 是 183KB 数据即代码，建议迁 `data/seed.json`，generate.py 读 JSON。
- 中优：`docs/papers.json` 与 `data/papers.json` 双写 3MB 副本；`norm_title`/`ALWAYS_LIST_VENUES` 在 py 与 js 重复需同步；`collect_2026.py` 产 dict 未接入自动合并。
- 前端：`papers.json` 含摘要整包 3MB 下发，建议拆出摘要懒加载；facets 计数可一次遍历算齐。

## 本次「逐步整理」执行记录
### Step 1 仓库卫生（已完成）
- 新建 `.gitignore`（忽略 __pycache__、2MB abstracts.json、各类备份/临时文件）。
- 修复 `audit.py` Benchmark 字段名 bug（evaluation→benchmark，现显示 114）。
- 修复 `generate.py` 缺失 `import sys`（incremental.json 损坏时本应跳过却崩溃）。

### Step 2 前端拆包（已完成，已验证）
- `generate.py`：papers.json 改紧凑 JSON（indent→separators，3.08→2.73MB）；新增
  `docs/papers.lite.json`（不含 abstract，首屏 0.86MB，−68%）；新增 `docs/abstracts.byid.json`
  （id→摘要，1390 条，后台懒加载，不阻塞首屏）；README "Last Update" 徽章改为动态 `datetime.now().strftime("%Y-%m")`。
- `app.js`：优先 fetch `papers.lite.json`，失败回退 `papers.json`；后台 fetch `abstracts.byid.json` 填充
  `state.abstracts`；搜索 haystack 与卡片摘要改用懒加载摘要；`node --check` 通过。
- `index.html`：加 `<link rel="preload" href="papers.lite.json" as="fetch">`。
- 验证：audit `data==docs md5` 仍 True、CCF 一致、Benchmark=114、无缺字段。
- 备注：docs 下现同时有 full(2.73MB)+lite(0.86MB)+map(1.86MB)；可选后续把 docs 的 full 也降为 lite 以消除摘要重复（需同步放宽 audit 的 md5 不变量）。

### 下一步
- Step 4：`raw_data.py` → `data/seed.json` 迁移（结构性，需改 generate.py 读取方式）。
- 可选：抽共享 `norm_title` 模块消除 5 处重复；facets 单次遍历计数（实际 6 遍遍历 1892 条开销极小，价值低，可跳过）。

### Step 3 venue_links 孤儿清理（进行中）
- 已备份 `data/venue_links.json` → `.bak`（已 gitignore）。
- 已清理 67 个正向孤儿（venue_links 有 key 但 papers 无匹配），剩 1655 key；audit 验证 `orphan_venue_links: 0`，硬不变量（md5/CCF/字段）全过。
- 分析：venue_links 共 1722→1655 key（796 dict 已解析 / 926 缓存负值""）；arXiv 预印本 978，其中 809 缓存负值、169 从未查询(uncached)；Pass A 全量仅 24/978 有 journal_ref/doi → 边际收益有限但值得跑。
- 后台重对齐 O4013O 因 `crossref_lookup` 未处理 Crossref `event` 字段为 dict 而 TypeError 崩溃（706/978）。已修复：对 event/book-title 增加 dict 类型处理（取 title/name 或忽略）。venue_links.json 未被破坏（VALID, 1655 keys），.bak 兜底。
- 重启重对齐时我自己的 Edit 引入缩进错误（`if not venue:` 块缺缩进）致 `8dEcTR`/vl_full2 立即 IndentationError；已修正（`py_compile` 通过），刷新 `.bak` 后重启后台重对齐 task **O3lvDH**（运行中，跑完自动通知）。完成后重跑 generate+audit 确认 venue 覆盖增益与 md5 不变量。

### Step 4 raw_data.py → data/seed.json 迁移（代码完成，待全流水线验证）
- 已把 `RAW`（1061 条 `(title,authors,url,year)` 元组）导出为 `data/seed.json`（JSON 数组，roundtrip 校验通过，与 RAW 完全相等）。
- `raw_data.py` 改写为**只读 shim**：加载 `seed.json` 并暴露 `RAW`（保持 4 元组格式），5 个只读脚本(generate/collect_2026/collect_candidates/enrich_links/fetch_abstracts) + merge_candidates 导入方零改动。
- `merge_candidates.py` 改为写 `data/seed.json`（JSON 数组）而非重建 `raw_data.py` 源码。
- `generate.py` 顶部注释更新（数据源 = seed.json）。
- `.gitignore` 未忽略 seed.json（它现在是应提交的数据源，正确）。三脚本 `py_compile` 通过。
- 数据层校验：shim 加载的 RAW == seed.json（1061 条全 4 元组）== 原 RAW。

### Step 3 + Step 4 最终全流水线验证（已完成）
- `update_arxiv_venues.py`(O3lvDH) 跑完：新解析 56/978，venue_links 1655→1817 key。
- `generate.py` + `audit.py` 验证全部通过：
  - `data/ == docs/ papers.json (md5): True`（Step4 迁移零回归）。
  - `CCF 评级与 ccf.json 一致: True (0 处不符)`；`venue_links 孤儿: 0`。
  - 总数 1892；含链接 1889、含摘要 1390、含代码 91、顶会正式发表 935（重对齐后 +56）；CCF-A 433/B 223/C 68；Benchmark 114；缺失字段 0、重复标题 0。
  - 前端产物随生成刷新：papers.lite.json 0.86MB、abstracts.byid.json 1.86MB；app.js 语法 OK。
- 结论：Step1 仓库卫生 ✅ / Step2 前端拆包 ✅ / Step3 venue 对齐 ✅ / Step4 seed.json 迁移 ✅ 全部完成。
- 剩余中低优项（可选，未做）：norm_title 抽共享模块(5处重复)、ALWAYS_LIST_VENUES 抽 facets.json、collect_2026.py 接入自动合并、code_common.py SSL 注释、facets 单次遍历计数。




## [自动化] arXiv 更新 (23:55)
- update_pipeline.py 退出码0，审计通过；本次新增0篇。
- 库总数1892；CCF-A433/B223/C68/未收录1168；含摘要1390。
