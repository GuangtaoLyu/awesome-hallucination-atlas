# 2026-07-29

## 仓库热点词落地（SEO / 蹭趋势，用户认可方案后执行）
- README 简介加热点词：EN "...across LLM/VLM/MLLM, **Agents**, and **RAG** — including reasoning-time hallucination"；ZH 同步（智能体/RAG/推理时）。
- 新增双语「🔥 热点方向 / Trending Directions」小节（generate.py `write_readme` 注入，属人工段落不被 `gen_readme.py` 覆盖）：Agentic AI / RAG-Faithfulness / Reasoning Models 三个方向 + 说明；TOC 加锚点 `#sec-trending`。
- Benchmark 区块（generate.py + gen_readme.py 两处）加 "including RAG faithfulness / groundedness benchmarks" 句。
- `data/facets.json` 新增 `trending_tags: ["Agent","RAG","Reasoning"]`；generate.py 新增 `_load_trending_tags()` 并注入 `stats.facets.trending_tags`（单一来源，前端复用）。
- 前端 `docs/app.js`：模块级 `TRENDING` 集合（从 `stats.facets.trending_tags` 读，回退默认三值）；tags 维度命中则在 chip 前加 🔥 + 靛蓝高亮（border #8b7cf6 / bg rgba(139,124,246,.18) / bold）。
- 校验：generate+gen_readme 重生成 OK（1898 篇）；`audit.py --strict` 全不变量 OK；`node --check app.js` OK；无混排标题。

## 社交预览图 social-preview.png 修图
- 背景改 AI 生成的深色星云/神经节点底图；crop 改 cover 方式（先等比放大覆盖 1280×640 再居中裁）消除右侧黑边；pill 文字改亮白 (240,243,252) 提升可读性，填充保留半透明靛蓝玻璃感。
- 复用脚本 `scripts/gen_social_preview.py`（AI 底图路径硬编码，换图需改路径）。

## 建议用户手动加的 GitHub Topics（非代码改动）
`llm` `hallucination` `mllm` `vlm` `agents` `agentic-ai` `rag` `reasoning` `benchmark` `survey` `trustworthy-ai` `multimodal`

## ⚠️ 用户否决"热点词仅加关键词"做法（重要偏好）
- 我上一轮在 README 文字里加了 Agents/RAG/Reasoning 词、网站给三标签加 🔥 高亮，但数据中 **RAG/Reasoning/Faithfulness 标签均为 0 篇**（`Agent` 116 篇是原有真实分类），属于"只加关键词、无真实分类"。用户指出后**全部回退**。
- **偏好结论**：不要为了蹭趋势往 README/网站里塞没有数据支撑的关键词或高亮；要么真的对论文做分类（标签有真实论文数），要么就别加。
- 回退范围：`generate.py`（删 `_load_trending_tags`/`trending_tags`/`🔥 热点方向` 小节/intro 加词/Benchmark RAG 句）、`gen_readme.py`（Benchmark RAG 句）、`docs/app.js`（删 TRENDING 集合与 🔥 高亮）、`data/facets.json`（删 trending_tags）。重生成后 `git diff --stat` 仅余 MEMORY.md，已干净还原到提交 8765f3b。

## 用户要求给 VLA/Agent/RAG/Reasoning 做热点标签（这次先查数据再决定）
- 查数据：`Agent` 标签 116 篇（原有真实）；`RAG`/`Reasoning`/`VLA` 标签均为 0，但标题含 "rag"/"reason" 的论文分别有 88/104 篇（说明论文存在但未被打标签）；`VLA` 标题命中 0 篇（语料库根本无 VLA 论文）。
- **做法**：在 `scripts/generate.py` 的 `extra_tags()` 加两条正则（与 `Agent` 同机制）——RAG: `\brag\b|retrieval[- ]augmented|retrieval[- ]based`；Reasoning: `reasoning|chain[- ]of[- ]thought|\bcot\b|deliberat|slow[- ]thinking|\bo1\b|deepseek[- ]r1`。重新生成后真实标签数 **RAG=158、Reasoning=371**（从摘要里也命中，比纯标题更多）。README 分类体系附加标签说明补 `RAG`/`Reasoning`（双语）。`audit --strict` OK。
- **VLA 结论**：字面 "vla" 0 篇、"vision-language-action" 1 篇——严格意义的 VLA 不值得成标签。用户指出后扩搜发现 world model 3 / embodied 11 / manipulation 12 / robot 4，但有重叠且多数已被 Agent 标签覆盖（12 篇）。用户从选项里选了**纯 "Embodied" 标签**（非 VLA、非 WorldModel）。
- 实现：`extra_tags()` 加 Embodied 正则 `\bembodied\b|\brobot` 或 (`manipulation` 且上下文含 robot/embodied/grasp/physical/act，排除 token manipulation 误命中)；README 附加标签补 `Embodied`（中文注"具身/机器人"）。重新生成后 **Embodied=19 篇**。`audit --strict` OK。
- 最终真实标签：Agent 116 / RAG 158 / Reasoning 371 / Embodied 19。VLA 始终未加。
- 用户称这些为"热点标签"，但本轮**只加了真实标签**（作为普通筛选 chip 出现），未复活此前否决的 🔥 trending 高亮 UI。是否要给这几个真实标签复活 🔥 高亮，待用户确认。
